

// const unsubscribe = onAuthStateChanged(auth, (user) => {
//     if (user) {
//         const uid = user.uid;
//         console.log("Auth changed to logged in! UserId: ", uid);
//         isLoggedIn = true;
//     } else {
//         console.log("Auth changed to not logged in");
//         isLoggedIn = false;
//     }
// });